const MODULE_CATEGORIES = ['core', 'work_management', 'communication', 'reporting', 'system'];
const MODULE_STATUSES = ['active', 'inactive', 'deprecated'];

const KEY_PATTERN = /^[a-z][a-z0-9_]*$/;

module.exports = {
  MODULE_CATEGORIES,
  MODULE_STATUSES,
  KEY_PATTERN,
};
