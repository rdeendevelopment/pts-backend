const { AI_MODELS } = require('./ai-models.constants');
const { EXECUTION_MODES } = require('./execution.constants');

/**
 * Config-driven AI action registry.
 * Feature modules call aiDispatcher.execute({ action, ... }) — never OpenAI directly.
 */
const AI_ACTION_REGISTRY = {
  TASK_SUMMARIZE: {
    action: 'TASK_SUMMARIZE',
    label: 'Summarize task',
    sourceModule: 'tasks',
    model: AI_MODELS.GPT_4O_MINI,
    temperature: 0.3,
    executionMode: EXECUTION_MODES.AUTO,
    timeout: 15000,
    maxSyncTokens: 3000,
    responseSchema: { type: 'object', required: ['summary'] },
    traceEnabled: true,
    saveLogs: true,
    promptKey: 'task.summarize',
  },
  TASK_BREAKDOWN: {
    action: 'TASK_BREAKDOWN',
    label: 'Break down task into subtasks',
    sourceModule: 'tasks',
    model: AI_MODELS.GPT_4O_MINI,
    temperature: 0.4,
    executionMode: EXECUTION_MODES.AUTO,
    timeout: 20000,
    maxSyncTokens: 4000,
    responseSchema: { type: 'object', required: ['subtasks'] },
    traceEnabled: true,
    saveLogs: true,
    promptKey: 'task.breakdown',
  },
  DISCUSS_SUMMARIZE_TOPIC: {
    action: 'DISCUSS_SUMMARIZE_TOPIC',
    label: 'Summarize discussion topic',
    sourceModule: 'discussflow',
    model: AI_MODELS.GPT_4O_MINI,
    temperature: 0.3,
    executionMode: EXECUTION_MODES.AUTO,
    timeout: 15000,
    maxSyncTokens: 4000,
    responseSchema: { type: 'object', required: ['summary', 'topics'] },
    traceEnabled: true,
    saveLogs: true,
    promptKey: 'discussflow.summarizeTopic',
  },
  DISCUSS_IMPORT_CHAT: {
    action: 'DISCUSS_IMPORT_CHAT',
    label: 'Import and extract discussion insights',
    sourceModule: 'discussflow',
    model: AI_MODELS.GPT_4O_MINI,
    temperature: 0.2,
    executionMode: EXECUTION_MODES.ASYNC,
    timeout: 120000,
    maxSyncTokens: 8000,
    responseSchema: {
      type: 'object',
      required: [
        'summary',
        'requirements',
        'questions',
        'decisions',
        'risks',
        'task_candidates',
        'next_actions',
      ],
    },
    traceEnabled: true,
    saveLogs: true,
    promptKey: 'discussflow.importChat',
  },
  DISCUSS_ANALYZE_MESSAGE: {
    action: 'DISCUSS_ANALYZE_MESSAGE',
    label: 'Analyze message for AI suggestions',
    sourceModule: 'discussflow',
    model: AI_MODELS.GPT_4O_MINI,
    temperature: 0.2,
    executionMode: EXECUTION_MODES.AUTO,
    timeout: 30000,
    maxSyncTokens: 4000,
    responseSchema: {
      type: 'object',
      required: [
        'summary',
        'requirements',
        'questions',
        'decisions',
        'risks',
        'task_candidates',
        'next_actions',
      ],
    },
    traceEnabled: true,
    saveLogs: true,
    promptKey: 'discussflow.analyzeMessage',
  },
  DISCUSS_EXTRACT_REQUIREMENTS: {
    action: 'DISCUSS_EXTRACT_REQUIREMENTS',
    label: 'Extract requirements from discussion',
    sourceModule: 'discussflow',
    model: AI_MODELS.GPT_4O_MINI,
    temperature: 0.2,
    executionMode: EXECUTION_MODES.AUTO,
    timeout: 30000,
    maxSyncTokens: 6000,
    responseSchema: { type: 'object', required: ['requirements'] },
    traceEnabled: true,
    saveLogs: true,
    promptKey: 'discussflow.extractRequirements',
  },
  DISCUSS_GENERATE_DOCUMENT: {
    action: 'DISCUSS_GENERATE_DOCUMENT',
    label: 'Generate document from discussion',
    sourceModule: 'discussflow',
    model: AI_MODELS.GPT_4O,
    temperature: 0.4,
    executionMode: EXECUTION_MODES.AUTO,
    timeout: 180000,
    maxSyncTokens: 8000,
    responseSchema: {
      type: 'object',
      required: ['title', 'document_type', 'content_markdown', 'sections'],
    },
    traceEnabled: true,
    saveLogs: true,
    promptKey: 'discussflow.generateDocument',
  },
  PROJECT_GENERATE_SCOPE: {
    action: 'PROJECT_GENERATE_SCOPE',
    label: 'Generate project scope',
    sourceModule: 'projects',
    model: AI_MODELS.GPT_4O_MINI,
    temperature: 0.35,
    executionMode: EXECUTION_MODES.AUTO,
    timeout: 45000,
    maxSyncTokens: 6000,
    responseSchema: { type: 'object', required: ['scope', 'deliverables'] },
    traceEnabled: true,
    saveLogs: true,
    promptKey: 'project.generateScope',
  },
  REPORT_SUMMARIZE: {
    action: 'REPORT_SUMMARIZE',
    label: 'Summarize report data',
    sourceModule: 'reports',
    model: AI_MODELS.GPT_4O_MINI,
    temperature: 0.3,
    executionMode: EXECUTION_MODES.AUTO,
    timeout: 30000,
    maxSyncTokens: 5000,
    responseSchema: { type: 'object', required: ['summary'] },
    traceEnabled: true,
    saveLogs: true,
    promptKey: 'report.summarize',
  },
  COMMON_TRANSLATE: {
    action: 'COMMON_TRANSLATE',
    label: 'Translate text',
    sourceModule: 'common',
    model: AI_MODELS.GPT_4O_MINI,
    temperature: 0.1,
    executionMode: EXECUTION_MODES.SYNC,
    timeout: 10000,
    maxSyncTokens: 2000,
    responseSchema: { type: 'object', required: ['translated'] },
    traceEnabled: true,
    saveLogs: true,
    promptKey: 'common.translate',
  },
  COMMON_REWRITE: {
    action: 'COMMON_REWRITE',
    label: 'Rewrite text',
    sourceModule: 'common',
    model: AI_MODELS.GPT_4O_MINI,
    temperature: 0.5,
    executionMode: EXECUTION_MODES.SYNC,
    timeout: 10000,
    maxSyncTokens: 2000,
    responseSchema: { type: 'object', required: ['rewritten'] },
    traceEnabled: true,
    saveLogs: true,
    promptKey: 'common.rewrite',
  },
};

const AI_ACTION_KEYS = Object.keys(AI_ACTION_REGISTRY);

module.exports = {
  AI_ACTION_REGISTRY,
  AI_ACTION_KEYS,
};
