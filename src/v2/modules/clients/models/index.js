const { ensureClientIndexes } = require('./client.model');
const { ensureClientContactIndexes } = require('./clientContact.model');

module.exports = {
  ensureClientIndexes,
  ensureClientContactIndexes,
};
