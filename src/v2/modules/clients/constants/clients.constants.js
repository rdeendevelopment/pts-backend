const CLIENT_STATUSES = ['active', 'inactive', 'archived'];
const CLIENT_TYPES = ['business', 'individual', 'internal'];

const DEFAULT_BILLING_CURRENCY = 'USD';
const DEFAULT_LIST_LIMIT = 20;
const MAX_LIST_LIMIT = 100;

module.exports = {
  CLIENT_STATUSES,
  CLIENT_TYPES,
  DEFAULT_BILLING_CURRENCY,
  DEFAULT_LIST_LIMIT,
  MAX_LIST_LIMIT,
};
